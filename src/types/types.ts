export type Dish = {
    id: number;
    name: string;
    description: string;
    price: number;
    category: number;
    info: string[];
    quantity: number;
};

export type Category = {
    id: number;
    name: string;
};
